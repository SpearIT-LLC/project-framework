# UAT Results — 2026-08-26

**Runbook:** [UAT-COMMANDS.md](UAT-COMMANDS.md)
**Plugin:** spearit-framework-dev 0.4.0 (dev-marketplace, `claude-local-marketplace/framework`)
**Tester:** Gary Elliott (with Claude Code)
**Consuming repo:** `SpearIT/Projects/framework-uat` (throwaway; no `workspaces/framework/`)

One row per test, filled as each test runs. Summary row goes into the runbook's Results
table when the run completes.

| Test | Result | Notes / cards to file |
|---|---|---|
| UAT-00 | PASS | Throwaway repo with one-line CLAUDE.md; all six skills loaded (`fw-new-workspace`, `fw-new-kb-domain`, `fw-contacts`, `fw-new-ops-record`, `fw-move`, `fw-troubleshoot`); no `workspaces/` pre-existing. **Note:** bare `/fw-new-workspace` reports `Unknown command` before resolving to the namespaced `/spearit-framework-dev:fw-new-workspace` — the short form is not registered as an alias. Note on owning FEAT. **Tester note:** asking for a purpose on a one-per-repo workspace (operations, and likewise kb) is redundant — the purpose should be pre-filled by the template rather than prompted. Note on owning FEAT. |
| UAT-01 | PASS | Tree exact (`agreements deliverables dist meetings poc reference requirements src tests` + README), no `contacts/`. README `Type: product`; living-vs-frozen requirements distinction stated; `deliverables/README.md` carries handed-over-and-accepted rule. AI asked for purpose; user answer written to README ("A trivial "hello world" PowerShell script."). No hand-made structure. |
| UAT-02 | PASS | Tree = `agreements deliverables meetings reference requirements`; no `src`. README `Type: project`, carries the "SOW is a project named for the SOW" guidance and the product-splits-out rule. AI asked purpose; written as "Sample statement of work (SOW) for BD." |
| UAT-03 | PASS | Exactly `open onhold closed meetings agreements reference` + README; no `intake/`, `deliverables/`, `contacts/`. Named form `operations east` refused (exit 1, "operations takes no name"), nothing created. AI asked purpose; written as "Operations activities." **Note:** operations README is bare (Type/Purpose only) — no type-guidance paragraph like product/project. Note on owning FEAT. **Tester note:** asking for a purpose on a one-per-repo workspace (operations, and likewise kb) is redundant — the purpose should be pre-filled by the template rather than prompted. Note on owning FEAT. |
| UAT-04 | PASS | Run as `KB licensing`; created fixed-name `workspaces/kb/` with README, INDEX listing `licensing`, and `licensing/{cookbook,faq,reference,research}` + domain README (reference = theirs / research = ours). AI asked INDEX one-liner and kb purpose; INDEX line filled from user answer ("Software licensing and license managers."). **Tester note:** kb purpose prompt is redundant (same as UAT-03) — user declined to author one; a generic default was written. Should be pre-filled by the template. Note on owning FEAT. |
| UAT-05 | PASS | (1) `application foo` → "type 'application' was renamed 'product' (TASK-197)", exit 1, nothing created. (2) `sow bd-sow-002` → "'sow' is not a type — an SOW is a project named for the SOW", exit 1. (3) `product` (no name) → AI asked "What should this workspace be called?" without running the script. (4) `product Widget` → refused, exit 1. Nothing hand-created; `workspaces/` unchanged. **Note:** on (4) the refusal came from the `[ -e ]` exists-check (message "already exists: .../Widget") because the Windows FS is case-insensitive; the script's dedicated case-insensitive loop (line ~104) sits *after* it, so it would only be exercised on a case-sensitive FS. Behavior is correct either way — observation only. **UX finding (card-worthy):** guard errors are not human-friendly. Script: drop card ids (`TASK-197`) from messages; do not dump the usage block on type errors; when the intent is inferable, add a "Did you mean: `/fw-new-workspace product foo`" line in *command* syntax (the user never runs the `.sh`). Command: when the script suggests a correction, ask the user to confirm and run the corrected form — do not just echo the error verbatim. Still never auto-create on a guess. (Ad-hoc: `webapp foobar` → "unknown type 'webapp'", exit 1, nothing created — for a truly unknown type the type list *is* the right output, so keep it there; the AI should then map the description to a type (web app → product) and offer it.) **Synonym finding:** `webapp database consoleapp desktopapp service app tool script library package pipeline` all → "unknown type", exit 1, nothing created. The script has no synonym handling (only the legacy `application`/`sow` redirects) and the command prompt does not instruct the AI to map a described artifact kind to a type — so the user experience depends on AI improvisation (this run: AI offered `product foobar`; a raw echo of the error is equally likely). Recommend: the command prompt states the mapping rule explicitly (any deliverable software/tool/service/db/pipeline → `product`; ask to confirm) so the behavior is deterministic; the script stays four-type strict. **Agreed design (tester + AI):** on `unknown type`, the command should not relay the error; the AI explains product vs project in a sentence each, proposes the likely type carrying the user's name ("Create `/fw-new-workspace product tradewinds`?"), falls back to a neutral "product or project?" when it cannot tell, and runs the script only after confirmation. Same interaction covers the `application`/`sow` redirects. |
| UAT-06 | PASS | Created `.claude/templates/workspaces/floor/custom-floor/.gitkeep` and `.../project/README.md` (`# __NAME__ (override)`); `project ovr-test` → tree = `custom-floor/` only, README reads `# ovr-test (override)`. Override used wholesale (no plugin floor folders merged in); plugin templates untouched. No `_PURPOSE_PENDING_` in the override README, so no purpose prompt. Override deleted after. **UX note:** the result (`custom-floor/` only) confused the tester — "wholesale" replacement means a repo wanting to add one folder must copy and maintain the whole plugin template tree. Suggest (1) the command/README states plainly that an override replaces the plugin templates entirely and to copy the full tree first; (2) the script prints "Using project template override: .claude/templates/workspaces/" when it takes that path, so an override run is distinguishable from a normal one. **BUG (ad-hoc, card-worthy):** with an override lacking a type overlay (`.claude/templates/workspaces/` has `floor/`+`project/` but no `product/`), `product missing-type` copies the floor, then fails at `cp -R "$TPL/product/."` with a raw `cp: cannot stat` error, exit 1 — leaving a half-built `workspaces/missing-type/custom-floor/` on disk. Script should validate `$TPL/floor` and `$TPL/$TYPE` exist *before* creating anything, with a message naming the missing overlay. **Design question raised:** should valid types be discovered from the template folders instead of the hard-coded `case`? **Potential improvement (back pocket, not now):** discover valid types by scanning `templates/workspaces/` for overlay folders, each carrying its own rules in a small manifest (e.g. `type.conf`: `named=`, `floor=`, `aliases=`). Script `case` collapses to "overlay exists → read conf"; usage text generated from the folders; a project override could then add types. `kb` stays the one hard-coded special case (delegates to the domain script). Revisit if the four types become limiting; would amend ADR-009 D3 (closed type list). |
| UAT-07 | PASS | From zero (kb temporarily moved aside): `hpc` created the `workspaces/kb/` shell (README + INDEX) and the `hpc` domain in one run, INDEX gained the hpc line, purpose pending. Tester then asked for a realistic run instead: original kb (with `licensing`) restored and `hpc` added alongside — script took the grow path ("Created domain: hpc", no shell recreated), INDEX appended in order, existing purpose untouched. **UX improvement (tester):** the one-liner / purpose questions should offer help — "enter a description, or say help and the AI drafts one from the name and context for you to edit". Applies to kb domains, products, and projects. |
| UAT-08 | PASS | `company` added; INDEX appended in order (licensing, hpc, company). `HPC` refused, exit 1: "domain already exists (names are case-insensitive): hpc" — names the existing domain. INDEX has exactly one line per domain. **Sanity note:** `company` is an ordinary domain by structure (standard 4-folder template, no special README); its only specialness is the `fw-contacts` convention that the registry lives at `kb/company/contacts/` — a folder no template seeds and no script creates (the AI makes it on first contact record). Sits against the "no hand-made structure" rule. Suggest `fw-new-kb-domain.sh` seeds `contacts/` (+ pointer README) when domain = company, or at minimum the domain README documents it. |
| UAT-09 | PASS | `kb/hpc/README.md` contains all five: provenance rule (reference = read-not-written, stale by release; research = curated-and-concluded, stale by refutation); conflict-is-a-finding note; kb-research vs project-hub boundary; licensing note for `reference/`; pointer to troubleshooting cases living in `research/` (ts-case.md + fw-troubleshoot). Human review by tester pending confirmation. |
| UAT-10 | PASS | No registry: exit 1, "Error: no contact registry at workspaces/kb/company/contacts / Create the company domain first (fw-new-kb-domain.sh company), then add contact records there from the plugin's templates/records/contact.md." Nothing generated. **Notes:** (1) the fix names the `.sh` and the `company` domain, but the `company` domain already existed here — the real gap was the missing `contacts/` folder + records; message should distinguish "no company domain" from "no contacts yet" and speak in command terms (`/fw-new-kb-domain company`, `/fw-contacts` add). (2) Invoked with args (`Fred Flintstone`) the script prints a usage line and exits 1; the command takes no arguments — an AI-facing rule should say "arguments are a request to add/change that contact (step 2), not script args". |
| UAT-11 | PASS | Two fake records created from `templates/records/contact.md` in `kb/company/contacts/` (`fred-flintstone.md`: customer, assigned widget + bd-sow-001; `barney-rubble.md`: spearit, assigned bd-sow-001 + bogus ghost-ws); empty optionals deleted. `fw-contacts` → Generated `bd-sow-001/CONTACTS.md` and `widget/CONTACTS.md`, each with the machine header and one `[Name](../kb/company/contacts/slug.md) — role` line per assignment; "Warning: assignments reference missing workspace(s): ghost-ws". No email/phone in any CONTACTS.md (grep-verified). Note: `contacts/` folder hand-created by the AI on first record (see UAT-08 sanity note). |
| UAT-12 | PASS | Deleted Fred's `widget` Assigned line; rerun → "Generated: bd-sow-001/CONTACTS.md", "Removed stale: workspaces/widget/CONTACTS.md", ghost-ws warning persists. Header guard verified ad-hoc: a hand-made `ovr-test/CONTACTS.md` (no machine header) survived a rerun untouched — only generated files are removed. Ad-hoc: `/fw-contacts Mr Slate widget mgr` → AI read args as "add contact", created `mr-slate.md`, rerun regenerated `widget/CONTACTS.md`. **FEAT suggestion (tester): master contact view.** The registry folder is the master list but has no generated view — unassigned people (allowed by the template) appear nowhere; no "who do we know at org X" answer; no cross-check for duplicates/stale assignments. Have `fw-contacts.sh` also emit `kb/company/contacts/INDEX.md` (machine header; per person: link, affiliation, role, assigned workspaces; grouped by affiliation; no email/phone). Same script, same run, no new command. **Registry format (tester question, reports/presentations need team lists):** keep Markdown per-record as the authored source (multi-valued Assigned + prose Notes; human-readable; diffs well); formalise the `**Field:** value` line grammar as a spec and add `fw-contacts --check` validation; emit structured exports from the same collected data (`contacts.csv`/`.json`, and per-workspace `CONTACTS.csv` for team lists). YAML front matter is the fallback if the grammar proves too loose; CSV as *source* rejected. |
| UAT-13 | FAIL | Tester verdict: FAIL until optional fields persist (blank, not deleted) and an update procedure exists — file a BUG card. Records otherwise meet the contract (required fields present; Activity `escalation` completes the sentence; no workspace names in Activity). **Template-contract finding (tester):** the "delete empty optional fields" rule has no machine basis — `fw-contacts.sh` parses only `# Name` and `**Assigned:**` (and already skips blank / `__` Assigned lines); Email/Phone/Activity/Notes are never read. Deleting fields leaves no update path: a later-arriving phone must be re-added by hand with the right name and order. Recommend: required fields filled; placeholder tokens (`__X__`) never survive; blank optional fields may stay as a prompt; delete only what cannot apply (extra Assigned lines). Add an explicit update procedure to `/fw-contacts` (`/fw-contacts <name>` → AI opens the record, edits per template, reruns) — also resolves the UAT-10 arguments note. **Observation:** contact records are not script-generated — the AI authors them from `templates/records/contact.md` (this run: written directly with the template's fields/order rather than literally copied). Shape fidelity rests on the AI following the template. Option: a small `fw-new-contact.sh <slug>` that copies the template, strips the comment header, and creates `contacts/` on first use — making creation deterministic (as `fw-new-ops-record.sh` does) and closing the hand-made-`contacts/` gap from UAT-08. |
| UAT-14 | PASS | Tester had renamed `workspaces/operations` → `operations-save` beforehand. `inc test` → exit 1: "Error: no operations workspace with flow folders at workspaces/operations — create it first: fw-new-workspace.sh operations". Nothing created. Note: same message theme as UAT-05/10 — fix is named in `.sh` form rather than `/fw-new-workspace operations`. |
| UAT-15 | (in progress) | `inc vpn-drops-nightly` → `open/INC-001-vpn-drops-nightly.md`, script filled ID/Kind/Opened, Closed/Resolution blank. **Finding (tester, card-worthy):** creating an INC/REQ should trigger an intake conversation, as a new FEAT/BUG does in the framework — the command's step 2 ("fill the record with the user") is too thin. For an INC collect the facts known at open time: symptom, when/pattern, impact, environment + recent changes, reported-by/customer ref, already tried, urgency/due, contacts. Unknowns recorded as unknown. For a REQ: requested outcome, requester, due, acceptance. Also: same "delete optional fields" rule as the contact template — UAT-13 objection applies. **Finding 2 (from the run):** intake facts ("Widget app crashes on clicking the thingy") did not match the pre-chosen slug `vpn-drops-nightly` — the slug is fixed by the script *before* the facts are known, and there is no rename path (renaming by hand breaks the never-by-hand rule). Command order should be: intake conversation first → AI proposes a symptom-first slug → script creates the record. Blank optionals kept in this record per tester preference (UAT-13). |
| UAT-16 | | |
| UAT-17 | | |
| UAT-18 | | |
| UAT-19 | | |
| UAT-20 | | |
| UAT-21 | | |
| UAT-22 | | |
| UAT-23 | | |
| UAT-24 | | |
| UAT-25 | | |
| UAT-26 | | |
| UAT-27 | | |
| UAT-28 | | |
| UAT-29 | | |

---

## RESUME MARKER — session ended 2026-08-26

**Next step:** UAT-15 is in progress. Resume by finishing INC-001 intake (control name, version/OS + recent changes, impact/workaround, already tried, due/ticket id, contacts) and writing the answers into `workspaces/operations/open/INC-001-vpn-drops-nightly.md`, then `/fw-new-ops-record req add-user-jdoe` (expect REQ-002, run the intake conversation), then UAT-16 onward.

**State of the consuming repo** (`SpearIT/Projects/framework-uat`, uncommitted working tree):
- `workspaces/widget` (product), `bd-sow-001` (project), `operations` (six flow folders, restored from tester's `operations-save` rename), `ovr-test` (UAT-06 evidence, override deleted), `kb/{licensing,hpc,company}` with INDEX lines filled.
- `kb/company/contacts/`: `fred-flintstone.md` (assigned bd-sow-001), `barney-rubble.md` (bd-sow-001 + bogus ghost-ws), `mr-slate.md` (widget). Generated views: `bd-sow-001/CONTACTS.md`, `widget/CONTACTS.md`.
- `operations/open/INC-001-vpn-drops-nightly.md`: title/what-happened filled from intake; slug mismatches content (finding logged); blank optionals kept.
- No `.claude/templates` override present.

**Tally so far:** UAT-00–12, 14 PASS; UAT-13 FAIL; UAT-15 in progress; UAT-16–29 not run.

**Cards to file from this run (see rows):** BUG: half-built workspace on missing override overlay (UAT-06). BUG: contact optional fields deleted / no update procedure (UAT-13). FEAT/UX: friendly guard errors + "did you mean" in command syntax, confirm-then-run, unknown-type product-vs-project guidance, no card ids in messages (UAT-05/10/14). FEAT: INC/REQ intake conversation, intake-before-slug (UAT-15). FEAT: master contact INDEX + CSV/JSON exports (UAT-12). FEAT: seed `contacts/` for the company domain (UAT-08). Notes: short-form command alias (UAT-00), operations/kb README purpose pre-filled (UAT-03/04), override-run message + docs (UAT-06), discovered types manifest idea — back pocket (UAT-06), help-drafting for descriptions (UAT-07).
